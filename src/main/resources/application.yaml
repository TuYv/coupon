spring:
  application:
    name: coupon
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/coupon?useUnicode=true&useSSL=false&characterEncoding=utf8
    username: root
    password: root1234
